{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "Dev",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "preLaunchTask": "Build"
    }
  ]
}
